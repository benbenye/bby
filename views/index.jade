extends layout

block content
	h1= title
	p Welcome to #{title}
	-if(success)
		p=success
		p 你好，
			a(href='user/perInfo') #{user.name}	
	-else if(success_out)
		p=success_out
	-if(error)
		p=error
	section.ebook
		div.hd
			h2 电子图书
		div.bd
			ul
				