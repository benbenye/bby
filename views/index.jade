extends layout

block content
	h1= title
	p Welcome to #{title}
	-if(success_log)
		p=success_log
		p 你好，
			a(href='user/perInfo') #{user.name}
	-else if(success_perInfo)
		p=success_perInfo
	-else if(error)
		p=error